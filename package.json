{
  "name": "formesome",
  "private": false,
  "scripts": {
    "clean": "npx lerna clean --yes",
    "bootstrap": "npx lerna bootstrap",
    "build": "npx lerna run build",
    "install": "npm run clean && npm run bootstrap && npm run build",
    "start:demo": "cd apps/demo && npm run start"
  },
  "devDependencies": {
    "jest": "^27.5.1",
    "lerna": "^4.0.0",
    "prettier": "^2.2.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.5"
  }
}
